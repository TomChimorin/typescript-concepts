const numbers: number[] = [1, 2, 4, 6];
const str: string[] = ['one', 'two', 'three'];
console.log(str);
console.log(numbers);

const items: string[] = [];
items.push('Keyboard');
console.log(items);

const numList: number[] = []
numList.push(20);
console.log(numList);

const items1: Array<number> = [1, 4, 5];
console.log(items);